server:
  port: 8085
spring:
  application:
    name: yanki-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/bank_yanki
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
