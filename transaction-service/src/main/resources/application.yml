server:
  port: 8084
spring:
  application:
    name: transaction-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/bank_transactions

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  cloud:
    config:
      uri: http://localhost:8888
